<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>SIGN UP</title>
    <style>
        /* Google fonts*/
        @import url("https://fonts.googleapis.com/css2?family=Anta&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap");
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: #f6f6f6;
            height: 100vh;
        }
        .form_container {
            width: clamp(360px, 80%, 530px);
            height: clamp(180px, 50%, 330px);
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            margin-bottom: 20px;
            border-radius: 8px;
            border: 2px solid #000000;
            font-family: "Anta", sans-serif;
        }

        .form_container form {
            display: flex;
            flex-direction: column;
            align-self: center;
            border-radius: 4px;
            border: 1px solid rgba(0, 0, 0, 0.81);
            padding: 10px;
            width: clamp(250px, 70%, 370px);
            height: clamp(120px, 70%, 195px);
        }

        .form_container input {
            width: clamp(215px, 92%, 345px);
            margin-left: 10px;
            height: 40px;
            font-size: clamp(14px, 3vw, 16px);
            color: #000000;
            border: none;
            background: transparent;
            border-bottom: 2px solid #000000;
            outline: none;
        }
        .form_container input::placeholder {
            color: #000000;
            font-family: "Anta", sans-serif;
        }
        h1 {
            color: #000000;
            font-size: clamp(24px, 6vw, 32px);
            position: absolute;
            top: 20%;
            font-family: "Anta", sans-serif;
        }
        #btn {
            background: #0166B1;
            font-family: "Anta", sans-serif;
            color: #ffffff;
            font-size: clamp(14px, 3vw, 16px);
            width: 70%;
            height: 30px;
            margin: 0 auto;
            border-radius: 5px;
            transition: 0.3s ease-out;
        }


        .account {
            text-decoration: none;
            color: snow;
            font-size: clamp(16px, 4vw, 18px);
            font-family: "Anta", sans-serif;
            transition: 0.3s ease-in-out;
        }
        .account:hover {
            color: black;
        }
        footer {
            position: absolute;
            bottom: 0;
            text-align: center;
            padding-top: 30px;
            justify-content: center;
            align-items: center;
            height: 70px;
            background: #0166B1;
            font-family: "Anta", sans-serif;
            width: 100%;
            color: black;
        }



    </style>
</head>
<body>
<h1>SIGN UP</h1>
<div class="form_container">
    <form action="/signup" method="post">
        <input
                type="text"
                id="username"
                name="username"
                placeholder="USERNAME"
                required
        /><br />
        <input
                type="email"
                id="email"
                name="email"
                placeholder="EMAIL"
                required
        /><br />
        <input
                type="password"
                id="password"
                name="password"
                placeholder="PASSWORD"
                required
        /><br />
        <input type="text" id="role" name="role" placeholder="ROLE" /><br />
        <input type="submit" value="Sign Up" id="btn" class="submit" />
    </form>
</div>
<a href="/login" class="account" style="color:black">Already have account?</a>
<footer>
    <p>Nurlanuly Baktiyar SE-2226</p>
</footer>
</body>
</html>
